"use client"
import React from 'react'
 function TaskPage() {
    // const response = await fetch('http://localhost:3000/api/tasks')
    // const tasks = await response.json()
    console.log("hey from a client ")
    // cline code is first rendered on the client side so we can use hooks here
    // then javascript code is send to  the browser and rendered on the client side
  return (
    <div>
      
    </div>
  )
}

export default TaskPage
